@model Models.KeyWords

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>GameEdit</title>
    <script src="@Url.Content("~/Scripts/jquery-1.8.2.min.js")" type="text/javascript"></script>
    <script src="@Url.Content("~/Scripts/jquery.validate.min.js")" type="text/javascript"></script>
    <script src="@Url.Content("~/Scripts/jquery.validate.unobtrusive.min.js")" type="text/javascript"></script>
    <script src="~/Scripts/jquery.unobtrusive-ajax.js"></script>
    <link href="~/Content/style/tableStyle.css" rel="stylesheet" />
    <script type="text/javascript">
        function afterEdit(data) {
            //关闭对话框，刷新表格
            if (data == "ok") {
                window.parent.window.afterEdit();
            }
        }
        function submitFrm() {
            $("form").submit();
        }
    </script>
</head>
<body>
    @using (Ajax.BeginForm("GameEdit", new AjaxOptions() { OnSuccess = "afterEdit" }))
    {

        <table style="margin-top:28px;margin-left:30px;" cellspacing="10">
            @*<input type="hidden" value="@Model.Id" id="id" name="id" />*@
          <tr>
              <th colspan="2" style="text-align:center;">修改关键字</th>
          </tr>
            <tr>
                <td>关键词名称</td>
                <td>
                    <input type="text" name="keyWords" value="@Model.Key" style="height:23px;width:200px;" />
                </td>
            </tr>
            @*<tr>
                <td>是否入库</td>
                <td>
                    <select name="isLibrary" id="isLibrary"  style="height:25px;width:200px;">
                        @if (Model.IsLibrary == "1")
                        {
                            <option selected="selected" value="1">是</option>
                            <option value="0">否</option>
                        }
                        else if (Model.IsLibrary == "0")
                        {
                            <option value="0" selected="selected">否</option>
                            <option  value="1">是</option>                         
                        }                    
                    </select>

                </td>
            </tr>*@        
        </table>
    }
</body>
</html>
